# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

LPF is a Python project currently in its initial state. The main entry point is `LPF.py`.

## Development

This project has no build system, test framework, or package manager configured yet. To run the main file:

```bash
python LPF.py
```
